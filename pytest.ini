[pytest]
# Scope collection to the real test suites. The blockchain suite (tests/) is the
# engine's fitness substrate; the engine's own tests live in tests_meta/.
# archive/ and .prometheus/ hold runtime lineage snapshots and must never be
# collected as tests.
testpaths = tests tests_meta
norecursedirs = archive .prometheus .git __pycache__ *.egg
